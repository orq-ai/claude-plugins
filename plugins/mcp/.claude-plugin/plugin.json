{
  "name": "orq-mcp",
  "description": "Orq MCP server. Gives Claude access to Orq platform tools — agents, analytics, traces, experiments, and more.",
  "version": "0.2.0",
  "author": {
    "name": "Orq.ai"
  },
  "license": "MIT",
  "keywords": ["orq", "claude-code", "claude-desktop", "mcp"],
  "clients": ["claude-code", "claude-desktop", "claude-cowork"],
  "userConfig": {
    "ORQ_API_KEY": {
      "description": "Your Orq API key (find it at orq.ai → Settings → API Keys)",
      "sensitive": true
    }
  },
  "mcpServers": "./.mcp.json",
  "desktopConfig": "./claude_desktop_config.json"
}
